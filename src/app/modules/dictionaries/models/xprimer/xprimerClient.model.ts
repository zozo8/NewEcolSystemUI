import { DefaultProperties } from 'src/app/models/defaultProperties.model';

export class XprimerClient extends DefaultProperties {
  name: string;
  symbol: string;
  nip: string;
  adress: string;
}
